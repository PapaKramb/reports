%h1 Проекты
%table
  %thead
    %tr
      %th №
      %th Название
      %th{:colspan => "2"}
  %tbody
    - @projects.each do |project|
      %tr
        %td= project.id
        %td= project.name
        %td
          = link_to "Редактировать", edit_admin_project_path(project), class: "btn btn-outline-warning"
          *-*
        %td
          = link_to "Удалить", admin_project_path(project), method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-outline-danger"
%br/
= link_to "Новый проект", new_admin_project_path, class: "btn btn-outline-info"
